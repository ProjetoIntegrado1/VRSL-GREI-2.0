using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class S_Rotacao_Mapa : MonoBehaviour


{
    public Transform targetz;

    private bool RotacaoPlayer = false;

    // Update is called once per frame
    void Update()
    {
        if (RotacaoPlayer == false && Input.GetKeyDown(KeyCode.R))
        {


        }
    }
}